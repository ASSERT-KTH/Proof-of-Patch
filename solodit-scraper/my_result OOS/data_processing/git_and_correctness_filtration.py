"""
extract all urls in jsonfile

for url in urls
    use beutifulsoup (or something else) to extract text from webpage
    prompt gpt api with text and prompt engineering
    if correct and git
        urls_to_include.append(url)
"""