# VeriSet

Instruction:
    docker build --no-cache --progress=plain -t your-image-name .

    if .env and docker-compose in root:
        docker compose build --no-cache --progress=plain

for me:
    symlink:
        ln -s ../../.env .env